# wiki/setona_hp/

`Wiki-hp` (Setona_HP Planner+ClaudeCode) の専用Vaultスペース。

## 状態

**構築予定** - `Wiki-hp` 起動コードと REX_HP_Brain NLM が稼働開始したのち本格運用開始。

現状は空フォルダとして配置(将来の専用スペース予約)。

## 用途(将来)

Setona_HP リポの設計判断記録・専門知見・運用ノウハウを格納する。Trade_System の `wiki/trade_system/` に相当する位置付け。

## 想定構造(参考・暫定)

```
wiki/setona_hp/
├── doc_map.md           # ドキュメント全体マップ
├── adr_reservation.md   # HP固有のADR予約管理
├── pending_changes.md   # 進行中の変更
└── ...

```
実際の構造は `Wiki-hp` 構築時に決定する。